#include <iostream>

class HtmlTag {
    public:
        //Tag(std::string name, std::string text, std::string src);
        //Tag();
        std::string name;
        std::string content;
        std::string src;
        std::string href;
        std::string toString();
};
